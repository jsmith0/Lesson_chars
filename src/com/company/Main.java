package com.company;

public class Main {

    public static void main(String[] args) {
        char ch = 'a';
        //can only hold 1 character
        String difference = "a";
        //can hold a ton of stuff
        char a = difference.charAt(0);
        System.out.println(a);
        //the program does not see them as the same, a character is not a string

        //ASCII (american standard code for information interchange) = the keyboard
        //the keys we hit gets transferred in numbers, registers as a decimal, goes to binary
        // A=65, a = 97 (KEY TO KNOW)
        
    }
}
